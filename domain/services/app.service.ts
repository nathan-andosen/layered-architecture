import { DI } from './dependency-injection.service';

@DI.Singleton('AppService')
export class AppService {


  /**
   * Initialise the app
   *
   * @memberof AppService
   */
  initializeApp() {
    
  }
}
